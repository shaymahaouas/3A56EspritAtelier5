<table border='2'>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        {% for author in author %}
            <tr>
                <td>{{ author.id }}</td>
                <td>{{ author.username }}</td>
                <td>{{ author.email }}</td>
                
                 <td><a href="{{path('editauthor',{'id':author.id})}}">edit</a>
        <a href="{{path('deleteauthor',{'id':author.id})}}">delete</a></td>

            </tr>
        {% endfor %}
    </tbody>
</table>